# Tech stack (locked)

Detailed companion to `AGENTS.md` section 4. Pin versions where they matter.

<!-- GENESIS:FILL — the interview writes one row per layer this project
actually has, using the columns below. Nothing is pre-listed on purpose. -->

| Layer | Choice | Version | SDK import module |
| ----- | ------ | ------- | ----------------- |

> For every vendor row, name the **one module** that imports its SDK (vendor
> isolation — `AGENTS.md` §3). Interface/adapter + contract test arrive with a
> second implementation; record that as an ADR when it happens.
