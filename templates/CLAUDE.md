# CLAUDE.md

This is a **thin wrapper**. The single source of truth is
**[`AGENTS.md`](./AGENTS.md)** — read it first.

- **Start of session:** read `AGENTS.md`, then the memory bank
  (`docs/process/ai-session-protocol.md` §Start of session).
- **End of session:** run the checklist in
  `docs/process/ai-session-protocol.md` §End of session.

All rules — the language policy included — live in `AGENTS.md`. This file is a
pure pointer, on purpose (pointers that carry content drift out of sync).
