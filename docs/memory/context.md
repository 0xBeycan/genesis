# Context

> Current working state and tribal knowledge for the next agent working on
> Genesis itself.

- **Stage:** v0.9.0 — dogfood-recalibrated. The product skeleton lives in
  `templates/`; the root is Genesis's own workshop; `GENESIS.md` is the
  build-first founder-interview protocol. Version: `CHANGELOG.md`.
- **Active constraints:** root files carry no placeholder syntax (CI-guarded);
  any `templates/` change ships with a `CHANGELOG.md` entry; no test runner
  here (docs-only repo) — verify guard/CI changes by running their commands
  locally.
- **Open questions:** v0.9.0 has not been validated end-to-end — the founder
  plans to re-bootstrap their real project from a fresh fork as its actual
  start; any remaining friction flows back here.
- **Hint for the next agent:** every rule has exactly one home — root
  `AGENTS.md` for the workshop, `templates/AGENTS.md` §3 for the product,
  `GENESIS.md` only for the interview/bootstrap process. The same policy now
  binds forks: after materialize their only Genesis reference is
  `context.md`'s skeleton-version line. Don't restate rules; point at them.
