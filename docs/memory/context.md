# Context

> Current working state and tribal knowledge for the next agent.

- **Stage:** Phase 0 — governance foundation from the Genesis skeleton.
- **Active constraints:** repo language is `{{REPO_LANGUAGE}}` (default English);
  everything written into the repo uses it.
- **Assets on hand:** the full governance skeleton (rules, docs, memory bank).
- **Open questions:** all interview answers (identity, vision, stack,
  architecture, design, roadmap) are still `{{...}}` placeholders.
- **Hint for the next agent:** don't recreate the skeleton — it exists. Run the
  `GENESIS.md` interview, then fill placeholders and generate the
  project-specific files.
